import React from 'react';

export const CommandLine = ({ lastPressed }) => {
  // TODO: implement this when numeric prefixes work, show command up to the
  // point it is completed with the movement of j and k, like in vim
  return null;
  // return <p>{lastPressed}</p>;
};
